rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for authenticated users (adjust for production)
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}